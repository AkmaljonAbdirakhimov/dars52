
class Note {
  final int id;
  String title;
  String content;
  DateTime created_time;

  Note({
    required this.id,
    required this.title,
    required this.content,
    required this.created_time,
  });
}
